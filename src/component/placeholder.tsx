import React from 'react';

const Placeholder: React.FunctionComponent = () => {
    return (
        <div style={{ padding: "20px", textAlign: "center", fontSize: "16px", color: "#aaa" }}>
    Loading...
    </div>
);
};

export default Placeholder;
